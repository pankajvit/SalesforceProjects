public with sharing class enforeSinglePrimaryContactOnAccountHdler {
    Set<Id> parentIds = new Set<Id>();
    public static void triggerMethod(List<Contact> conList, Map<Id,Contact> oldConMap){
        if(!conList.isEmpty()){
            for(Contact con : conList){
                
            }
        }
    }
}